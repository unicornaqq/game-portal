# Daily Conversation Summary - 2026-03-09

## Overview
This document contains a summary of conversations with OpenClaw assistant.

## Raw Conversation Log
```
# 2026-03-09

## 决策

1. **招聘逆向分析工具上线** - 创建了 `projects/job-intel/` 项目，每日分析一家公司的招聘信息，推断产品方向和痛点，已添加 cron job `job-intel-daily`（每天 9:00 发送到 #系统构建 频道）

2. **智兔 Token 策略** - 对比免费版和付费版后，发现两者功能相同（技术指标 API 都需要用日期范围参数），已切换回付费版 Token

## 项目状态

### 已完成
- ✅ 招聘逆向分析工具 (`projects/job-intel/`)
- ✅ MCP 技术指标修复（使用日期范围参数 `st=&et=` 替代 `lt` 参数）
- ✅ OpenClaw 架构每日一课第 6-7 课完成

### 进行中
- 🔄 多个 cron 任务失败（`cron announce delivery failed`），需排查
- 🔄 mcporter 与 stock-tracker MCP 兼容性问题（STDIO 模式超时，baseUrl 模式 404）

### 待办
- [ ] 心跳任务改用 Python 直接调用 MCP（绕过 mcporter）
- [ ] 修复「查 Reddit」时错误使用 Get Notes 的问题

## 经验教训

1. **智兔 API 参数差异** - `lt` 参数返回 null/0，必须用日期范围参数 `st=&et=` 才能获取技术指标数据

2. **mcporter 兼容性** - `StreamableHTTPServerTransport` 与 mcporter 的 STDIO 和 baseUrl 模式都不兼容，需改用直接 HTTP 调用

3. **BM25 算法** - SQLite FTS5 内置 BM25 排序函数，是「相关性打分」而非简单的等于匹配

4. **Embedding 维度** - 由模型决定，不需要手动指定（如 text-embedding-3-small = 1536 维）

## 阻塞问题

1. **Cron 任务批量失败** - 多个任务状态为 `error`，错误信息 `cron announce delivery failed`，需进一步排查根因

2. **知识库频道消息无响应** - 用户反映发到知识库频道的消息没有被回复，可能与任务失败相关

## 技术笔记

### Embedding & 向量化
- Embedding = 文字 → 向量
- 维度由模型固定（OpenAI 1536，本地模型 384-768）
- 向量距离近 = 语义相似

### BM25 & SQLite FTS5
- BM25 是相关性排序算法，输出连续分数
- 考虑：词频(TF)、逆文档频率(IDF)、文档长度归一化
- SQLite FTS5 内置 `bm25(表名)` 函数

### OpenClaw 混合搜索
- 并行执行：向量搜索 + BM25 关键词搜索
- 加权合并：`finalScore = 0.7×向量 + 0.3×BM25`
- 兼顾语义理解和精确匹配
```
## Metadata
- **Date**: 2026-03-09
- **Generated by**: OpenClaw Daily Conversation Logger
- **Repository**: https://github.com/unicornaqq/game-portal
